const rightOnlyMetrics = {
  branch: "refined-ui",
  filesChanged: 18,
  reviewers: ["qa", "design-review"],
  notes: [
    "adds persisted theme mode",
    "improves unified diff readability",
    "keeps toolbar buttons in place",
  ],
  deployment: {
    channel: "release-candidate",
    artifact: "diffly-refined.zip",
    signed: true,
  },
};

export default rightOnlyMetrics;
